<?xml version="1.0" encoding="UTF-8"?>
<ViewCell
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MCWeather.Forms.Views.LocationViewCell">
	<Grid
        Padding="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Label
            Grid.Row="0"
            Grid.Column="0"
            Text="{Binding City.Name}"
            TextColor="Black"
            FontSize="Medium"/>
        <Label
            Grid.Row="1"
            Grid.Column="0"
            Text="Current Location"
            TextColor="Gray"
            FontSize="Micro" />
        <Label
            Grid.Row="0"
            Grid.Column="1"
            Grid.RowSpan="2"
            VerticalOptions="Center"
            Text="{Binding CurrentTemperature}"
            TextColor="Black"
            FontSize="Large" />
    </Grid>
</ViewCell>
